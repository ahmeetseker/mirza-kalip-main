import styled from "styled-components";
import { View, Text } from "components";

const TeamContent = styled(View)`
  text-align: center;
  padding: 0 30px 30px;
  border: 1px solid #dedede;
  border-top: none;
  transition: all 300ms linear 0ms;
`;

export default TeamContent;
