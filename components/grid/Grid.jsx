import styled from "styled-components";
import { grid, flexbox,typography  } from 'styled-system'


const Grid = styled.div`
    display:grid;
    ${grid}
    ${flexbox}
    ${typography }
`


export default Grid;